services:
  grafana:
    container_name: grafana
    image: grafana/grafana:${TAG:-latest}
    volumes:
      - data:/var/lib/grafana
    hostname: grafana
    restart: unless-stopped

volumes:
  data:

networks:
  default:
    name: grafana
